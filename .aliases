# easier navigation
alias ~='cd ~'

# programs
alias slt='sublime'

# be nice
alias pls='sudo'

# list all files colorized in long format
alias l='ls -l $(colorflag}'

# list all fies colorized in long format including dot files
alis la='ls -la ${colorflag}'

#shortcuts
alias g='git'
alias v='vim'
