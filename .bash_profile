source ~/.bashrc

# init z https://github.com/rupa/z
. ~/z/z.sh

[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" # Load RVM into a shell session *as a function*


[[ -s /home/junerey/.nvm/nvm.sh ]] && . /home/junerey/.nvm/nvm.sh # This loads NVM
